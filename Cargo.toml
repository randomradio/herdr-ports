[package]
name = "herdr-ports"
version = "0.2.1"
edition = "2021"
license = "MIT"
description = "Forward a Herdr workspace port to herdr.{workspace}.localhost:{port}"

[dependencies]
anyhow = "1"
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
